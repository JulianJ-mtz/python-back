{
  "version": 2,
  "buildCommand": "pip install --upgrade pip poetry && poetry export -f requirements.txt --output requirements.txt --without-hashes && python -m pip install -r requirements.txt -t .python_packages/lib/python3.11/site-packages",
  "outputDirectory": ".",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ],
  "env": {
    "PYTHONPATH": "/var/task/.python_packages/lib/python3.11/site-packages:/var/task"
  }
}